{
  "name": "etchr",
  "version": "4.0.0",
  "description": "Etchr — an embeddable, framework-free visual HTML/CSS editor",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "watch": "node esbuild.config.mjs --watch",
    "serve": "node server/server.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
